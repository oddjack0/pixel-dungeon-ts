{
  "name": "pixel-dungeon-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build src/main.ts --outdir dist --target browser && cp index.html dist/index.html"
  },
  "devDependencies": {
    "bun-types": "^1.4.2",
    "typescript": "^5.6.0"
  }
}
